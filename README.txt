en el directorio de frontend hay un archivo .html que cree a modo de ejemplo para la inscripcion y para ver su funcionalidad, pueden modificarlo o borrarlo a gusto. si lo usen acomodenlo dentro del directorio correspondiente

para el backend: el directorio de middleware sirve para configuraciones intermediarias dentro del proyecto, como la conversion del archivo comprobante que se requiere. el directorio config para configuraciones como la base de datos, y la de services para servicios je

en el .env van variables de entorno, osea credenciales sensibles q no se deben filtrar

qsyo q mas añadir nunca escribe esto
